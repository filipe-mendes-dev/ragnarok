export function SiteFooter() {
    return (
        <footer className="border-t border-border">
            <div className="mx-auto flex min-h-16 max-w-6xl flex-col justify-center gap-1 px-5 py-4 text-xs text-muted-foreground sm:flex-row sm:items-center sm:justify-between sm:gap-4 sm:px-8">
                <span>RAGnarok</span>
                <span>Grounded answers. Inspectable retrieval.</span>
            </div>
        </footer>
    );
}
